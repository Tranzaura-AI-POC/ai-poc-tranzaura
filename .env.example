# Example environment variables for running Playwright tests locally
# Copy this file to `.env` and fill in real values (DO NOT commit `.env`).

# Admin user credentials used by tests — replace with your values (do NOT commit `.env`)
# Example: FLEET_USERNAME=admin
FLEET_USERNAME=your_test_username
# Example: FLEET_PASSWORD=Password123!
FLEET_PASSWORD=your_test_password

# Backend and frontend hosts (optional override)
# FLEET_API_URL=http://127.0.0.1:5000
# FLEET_FRONTEND_URL=http://127.0.0.1:4200
